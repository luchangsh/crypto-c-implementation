generating prime numbers using the multiple precision math library (called BIGNUM) of openssl and modular arithematics with BIGNUM

Compile:
	make hw6

======================================
Known Bugs:
	None

======================================
Used Libraries:
	OpenSSL/BIGNUM
